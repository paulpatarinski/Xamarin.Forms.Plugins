<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:abstractions="clr-namespace:ExtendedCells.Forms.Plugin.Abstractions;assembly=ExtendedCells.Forms.Plugin.Abstractions"
             x:Class="SampleApp.App">
	<Application.Resources>
    <ResourceDictionary>

      <Color x:Key="White">White</Color>
      <Color x:Key="Red">Red</Color>
      
      <Style TargetType="abstractions:TwoColumnCell">
        <Setter Property="RightTextColor" Value="{StaticResource Red}"/>
      </Style>
      <Style x:Key="CellWithThickness" TargetType="abstractions:TwoColumnCell">
        <Setter Property="BackgroundColor" Value="{StaticResource Red}"/>
        <Setter Property="LeftTextColor" Value="{StaticResource White}"/>
        <Setter Property="Thickness" >
          <Setter.Value>
            <Thickness>50,50,50,50</Thickness>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="CellWithBackground" TargetType="abstractions:TwoColumnCell">
        <Setter Property="BackgroundColor" Value="{StaticResource Red}"/>
        <Setter Property="LeftTextColor" Value="{StaticResource White}"/>
      </Style>
    </ResourceDictionary>
    </Application.Resources>
  </Application>