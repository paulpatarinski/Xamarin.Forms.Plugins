<?xml version="1.0" encoding="UTF-8"?>
<ContentPage 
xmlns="http://xamarin.com/schemas/2014/forms" 
xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
xmlns:abstractions="clr-namespace:SVG.Forms.Plugin.Abstractions;assembly=SVG.Forms.Plugin.Abstractions"
x:Class="SampleApp.ViaResources" Title="Via Resources">
	
	<ContentPage.Resources>
		<ResourceDictionary>
			<abstractions:SvgImage x:Key="CoolMask" SvgAssembly="{Binding SvgAssembly}" SvgPath="{Binding CoolMaskSvgPath}" HeightRequest="50" WidthRequest="50" BackgroundColor="White" HorizontalOptions="Center" VerticalOptions="Center"/>
			<abstractions:SvgImage x:Key="CoolMask2" SvgAssembly="{Binding SvgAssembly}" SvgPath="{Binding CoolMaskSvgPath}" HeightRequest="50" WidthRequest="50" BackgroundColor="White" HorizontalOptions="Center" VerticalOptions="Center"/>
		</ResourceDictionary>
	</ContentPage.Resources><ContentPage.Content>

	<StackLayout>
		<ContentView BackgroundColor="Red" HeightRequest="150" WidthRequest="150" Content="{StaticResource CoolMask}"/>
		<ContentView BackgroundColor="Red" HeightRequest="100" WidthRequest="100" Content="{StaticResource CoolMask2}"/>
	</StackLayout>

	</ContentPage.Content>
</ContentPage>
